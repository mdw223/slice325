baseURL = '/'
languageCode = 'en-us'
title = 'Slice325 Learn'
theme = "hugo-theme-learn"
# For search functionality
[outputs]
home = [ "HTML", "RSS", "JSON"]
[minify]
  disableHTML = true

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true